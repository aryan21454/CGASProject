import React from 'react'

const RecipeData = ({queryResult}) => {
  return (
    <div>
      {
        // queryResult && queryResult.map((key, index)=>{
            
        // })  
      }
    </div>
  )
}

export default RecipeData
